const $text = document.querySelector('#code-text');

// TODO: on button click, reshape contents of $text
// TODO: probably want to run input through babel to cleanup any too-fancy syntax
// TODO: move this to gh-pages branch?
